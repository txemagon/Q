<%= form_for(@register_model, html: { multipart: true }) do |f| %>
  <% if @register_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@register_model.errors.count, "error") %> prohibited this register_model from being saved:</h2>

      <ul>
      <% @register_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :explanation %><br>
    <%= f.text_area :explanation %>
  </div>
  <div class="field">
    <%= f.file_field :document %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
